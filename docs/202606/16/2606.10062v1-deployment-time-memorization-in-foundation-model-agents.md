---
title: Deployment-Time Memorization in Foundation-Model Agents
title_zh: 基础模型代理中的部署时记忆化
authors: "Lei, Chen, Guilin Zhang, Kai Zhao, Dalmo Cirne, Andy Olsen, Xu Chu, Zeke Miller, Alet Blanken, Amine Anoun, Jerry Ting"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10062v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 研究基础模型智能体中部署时记忆问题，分析记忆设计选择
tldr: "长期智能体记忆在部署时面临隐私-效用权衡。现有研究未联合分析记忆设计选择的影响。我们提出部署时记忆框架，扫描摘要激进度、检索广度k和删除模式三个旋钮，并引入遗忘残留分数FRS。关键事实摘要将提取率降低76%和64%且保持个性化，但仅原始删除导致约20%残留，需全管道清除才能消除。持久记忆必须作为第一类机制评估。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10062v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 819, \"height\": 967}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10062v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 556}, {\"url\": \"assets/tables/arxiv/2606.10062v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1497, \"height\": 542}]"
motivation: 现有工作未联合分析记忆设计选择对个性化效用、提取风险和删除保真度的影响。
method: 将智能体记忆建模为隐私-效用前沿，通过PR和AER测量，扫描三个设计旋钮，提出FRS量化删除残留。
result: "关键事实摘要降低提取率76%（Gemma 3 12B）和64%（GPT-4o-mini）且保留个性化，但仅原始删除导致约20%残留，全管道清除可消除。"
conclusion: 持久智能体记忆必须作为第一类记忆机制评估，需同时考虑回忆、提取和删除能力。
---

## 摘要
基础模型代理日益成为跨交互记住用户的长期系统，使得记忆化成为明确的部署时功能，而不仅仅是模型权重的属性。现有工作关注参数化记忆或审计固定内存配置，但未描述内存设计选择如何共同塑造个性化效用、提取风险和删除忠实度。我们将这一表面作为部署时记忆化进行研究，将代理内存表述为由个性化召回（PR）和对抗性提取率（AER）度量的隐私-效用前沿，并遍历三个内存设计旋钮：摘要攻击性、检索广度（k）和删除模式。我们进一步引入遗忘残差分数（FRS）以量化删除信息是否仍可从派生内存层级中恢复。在LongMemEval上，关键事实摘要将Gemma 3 12B的金丝雀提取减少76%，GPT-4o-mini减少64%，同时几乎保留所有个性化召回；关键是，一旦内容被压缩掉，增加k不再恢复泄漏。然而，相同的压缩会导致删除忠实度失败：仅原始删除使派生摘要副本在大约20%的实例中可恢复，只有全管道清除或墓碑修订能将最差层级残差降至零。这些结果共同表明，持久代理内存必须作为一等记忆化机制进行评估——通过它帮助代理回忆的内容、它使什么可提取以及它能真正擦除什么来评估。

## Abstract
Foundation-model agents are increasingly long-lived systems that remember users across interactions, making memorization an explicit deployment-time function rather than solely a property of model weights. Existing work addresses parametric memorization or audits fixed memory configurations, but does not characterize how memory-design choices jointly shape personalization utility, extraction risk, and deletion fidelity. We study this surface as deployment-time memorization, formulating agent memory as a privacy-utility frontier measured by Personalization Recall (PR) and Adversarial Extraction Rate (AER), and sweeping three memory-design knobs: summarization aggressiveness, retrieval breadth (k), and deletion mode. We further introduce the Forgetting Residue Score (FRS) to quantify whether deleted information remains recoverable from derived memory tiers. On LongMemEval, key-fact summarization reduces canary extraction by 76% on Gemma 3 12B and 64% on GPT-4o-mini while preserving nearly all personalization recall; critically, once content is compressed away, increasing k no longer restores leakage. The same compression, however, induces a deletion-fidelity failure: raw-only deletion leaves derived summary copies recoverable in approximately 20% of instances, and only full-pipeline purge or tombstone redaction drives worst-tier residue to zero. Together, these results establish that persistent agent memory must be evaluated as a first-class memorization mechanism -- assessed by what it helps agents recall, what it makes extractable, and what it can truly erase.

---

## 论文详细总结（自动生成）

# 论文总结：基础模型代理中的部署时记忆化

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题背景**：基础模型代理正从无状态助手演变为长期记忆用户的系统。现有研究主要关注模型权重中的参数化记忆（parametric memorization）或审计固定内存配置，但没有联合分析内存设计选择如何共同影响个性化效用、提取风险和删除保真度。
- **核心问题**：在持久化代理中，记忆化不仅是模型权重的偶然属性，更是系统性的部署时功能。这种“部署时记忆化”需要被测量和控制，以平衡隐私与效用。
- **研究动机**：填补现有工作空白，将代理内存设计形式化为一个隐私-效用前沿，并系统评估摘要压缩、检索宽度和删除模式三个设计旋钮的影响。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将代理内存视为一个写-检索-响应管道（write–retrieve–respond），并定义三个可调旋钮：摘要攻击性S、检索广度k、删除模式。通过测量个性化召回（Personalization Recall, PR）和对抗性提取率（Adversarial Extraction Rate, AER）来绘制隐私-效用前沿。
- **关键术语与公式**：
  - **个性化召回（PR）**：代理正确回答需要记忆用户上下文的问题的概率。答案正确性由余弦相似度>0.5或内容词完全匹配判定。
  - **对抗性提取率（AER）**：攻击者通过直接、间接或越狱提示从代理响应中恢复高熵金丝雀（canary）的概率。AER定义为金丝雀被逐字恢复的比例。
  - **隐私-效用AUC（PUA）**：在不同k下，对每个AER阈值取最大PR，然后积分得到面积，衡量给定压缩策略下隐私与效用的综合表现。
  - **遗忘残差分数（FRS）**：删除操作后，每个内存层级（raw/summary）上金丝雀仍可被提取的概率。报告最差层级残差FRS_worst = max_t FRS_t。
- **删除模式阶梯**：5种模式——noop（不变）、raw only（仅删除原始文本）、raw plus resummarize（删除并重新摘要）、full purge（全管道清除）、tombstone（替换为[REDACTED]并重新嵌入）。
- **算法流程**：用户会话→写入记忆→摘要压缩→检索top-k→注入上下文→模型响应。威胁模型假设攻击者可查询但不可直接访问记忆存储。

## 3. 实验设计：数据集、场景、基准与对比方法

- **数据集**：使用LongMemEval的oracle split，包含多会话聊天历史及需要长期记忆的问答对。对每个实例注入一个合成金丝雀，格式为“my private session token is [value]”，高熵生成（约5.6×10^9种可能）。
- **场景与基准**：
  - 主要实验在Gemma 3 12B（本地Ollama部署）上进行，并在GPT-4o-mini上复制S∈{0,1}的切片。
  - 对比不同摘要水平（S=0:原始对话轮次, S=1:关键事实, S=2:单句摘要）和不同检索广度k∈{1,3,6,10,25}。
  - 删除保真度实验覆盖所有五种删除模式，分别在S=0,1,2下评估FRS。
- **对比方法**：无显式基线方法对比，而是系统内对比不同配置下的PR/AER曲线和FRS值。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量、训练时长**。仅提到Gemma 3 12B通过Ollama在本地提供服务，GPT-4o-mini通过API调用。未提及训练过程（仅推理/部署阶段）。实验开销较小，主要涉及推理和嵌入计算（all-MiniLM-L6-v2）。

## 5. 实验数量与充分性

- **实验数量**：
  - 主实验：N=50个实例，全因子遍历S=0,1,2和k=1,3,6,10,25（共3×5=15种配置）在Gemma 3 12B上；再加S=0,1在GPT-4o-mini上（2×5=10种配置）。
  - 删除保真度实验：在每种S下遍历5种删除模式，重复对抗探针，得到FRS结果。
  - 探针类型：直接、间接、越狱三种，报告AER分解。
- **充分性与公平性**：
  - 样本量N=50较小，但作者使用95% bootstrap置信区间（1000次重采样）增强统计可靠性。
  - 对比了两个不同模型家族（开源Gemma，闭源GPT-4o-mini）以区分管道效应与模型特定效应，增强了外部有效性。
  - 消融实验覆盖了摘要、检索宽度、删除模式的完整组合，评估维度全面。
  - 未做跨不同数据集或更长交互历史的实验，可能限制了泛化性。

## 6. 论文的主要结论与发现

- **摘要压缩大幅降低提取风险**：关键事实摘要（S=1）将AER降低76%（Gemma）和64%（GPT-4o-mini），而对PR的损失仅约5%和2个百分点。单句摘要（S=2）进一步降低AER但PR损失更大，S=1是最佳折中。
- **检索广度k在压缩后不再影响泄露**：当摘要压缩去除金丝雀后，即使增加k也无法恢复泄露（k-flatness）。
- **删除保真度存在严重不足**：仅删除原始记录（raw only）在S=1时FRS_worst≈0.20，摘要副本仍可恢复。重新摘要可降低但无法清零；只有全管道清除或墓碑修订才能使FRS_worst归零。
- **直接与间接探针在压缩后行为一致**：S=1下直接和间接AER差异从0.16（Gemma, S=0）降至0.00；越狱探针受模型RLHF影响，GPT-4o-mini在S=0下已近零，但Gemma在S=1后才降低。
- **实际配方**：关键事实摘要 + 适度k + 层级感知的删除（全管道清除或墓碑修订）可平衡隐私与效用。

## 7. 优点：方法或实验设计上的亮点

- **系统性形式化**：首次将代理内存的隐私-效用权衡建模为可量化的前沿，并定义统一度量PR、AER、PUA和FRS。
- **多维旋钮消融**：同时扫描摘要、检索宽度和删除模式，揭示了各因素间的交互作用（如k在压缩后失效）。
- **威胁模型全面**：三种探针类型（直接、间接、越狱）覆盖实际攻击场景；删除保真度区分不同层级（raw/summary），揭示管道内拷贝问题。
- **跨模型验证**：在两种独立训练的模型上复现核心结论，确认结果是管道级效应而非模型特定偏差。
- **可复现性**：使用公开数据集LongMemEval、开源嵌入模型all-MiniLM-L6-v2、明确参数设置。

## 8. 不足与局限

- **样本量偏小**：仅50个实例，虽然bootstrap提供了置信区间，但可能无法覆盖真实世界用户交互的多样性。
- **金丝雀设计过于简单**：高熵字符串形式单一，真实用户秘密可能更复杂或结构化，提取难度可能不同。
- **未考虑更长交互序列**：实验为单次会话+一次遗忘操作，现实代理可能经历数百轮交互，累积记忆和派生副本更多。
- **仅评估两种模型**：Gemma 3 12B和GPT-4o-mini，虽横跨开源/闭源，但未覆盖更大规模（如70B+）或不同架构（如Mamba）。
- **删除保真度仅测立即效应**：未评估多次写入、多次删除或重复遗忘后的累积残留风险。
- **未涉及差分隐私等防御**：文中提到差分隐私是正交方向，但实验未包含。实际部署可能结合多种隐私技术。
- **实用性考虑不足**：全管道清除和墓碑修订在工程实现上可能带来高延迟或上下文丢失，论文未讨论成本。

（完）
