---
title: "Arbor: Tree Search as a Cognition Layer for Autonomous Agents"
title_zh: Arbor：树搜索作为自主智能体的认知层
authors: "Neha Prakriya, Chaojun Hou, Zheng Gong, Huasha Zhao, Xi Zhao, Mou Li, Zhenyu Gu, Emad Barsoum"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12563v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 维护显式搜索树作为多智能体的共享工作记忆
tldr: "现有自主优化系统通常在孤立目标上操作且缺乏状态记忆，导致在大型有状态动作空间中性能受限。本文提出Arbor多智能体框架，引入结构化树搜索作为认知层，维护显式搜索树作为共享工作记忆，并通过Orchestrator与Critic的制衡机制实现全栈自动优化。在LLM推理优化任务上，Arbor实现了高达193%的吞吐量-延迟帕累托改进，远超单智能体的33%提升，且运行方差小于2%。该方法硬件无关且可复现，为自主代理的认知架构提供了新范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12563v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1405, \"height\": 485, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1447, \"height\": 520, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1450, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1142, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1418, \"height\": 309, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1447, \"height\": 444, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.12563v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1424, \"height\": 422, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.12563v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1325, \"height\": 341, \"label\": \"Table\"}]"
motivation: 现有自主代理在大型有状态动作空间中缺乏结构化记忆和协调，导致优化效率低且易崩溃。
method: 提出Arbor多智能体框架，以树搜索作为认知层，Orchestrator驱动优化，Critic保障稳定性，分解硬技能与软技能实现自主协作。
result: "在LLM推理优化上，Arbor达到193%帕累托改进，单智能体仅33%并崩溃，且跨硬件平台方差低于2%。"
conclusion: 结构化树搜索可作为自主代理的通用认知层，实现硬件无关且可复现的全栈自动优化。
---

## 摘要
Arbor是一个多智能体框架，将结构化树搜索作为在大型有状态动作空间中运行的自主智能体的认知层。先前的自主优化系统在具有无状态评估的孤立目标上运行。而Arbor维护一个显式的假设评分搜索树，作为智能体间的共享工作记忆，随每次测量而演化，将失败视为重塑后续探索的诊断信号，并随着先前的成功改变瓶颈分布而扩展。我们在全栈大语言模型推理优化上验证了Arbor，该领域历史上需要跨应用、框架、编译器、内核和硬件栈的工程团队协调努力才能达到峰值性能。Arbor将协调智能体（通过委托给推理栈各领域的专家来驱动优化）与批评智能体（通过根因分析、内省和测量验证来保障稳定性）配对——这是一个制衡架构，其中任何智能体都不能单方面驱动系统。智能体能力分解为硬技能（领域专业知识）和软技能（决定贡献如何组合的协调协议），实现了完全自主的持续数天的活动。与供应商优化的基线相比，Arbor实现了高达193%的推理吞吐量-延迟帕累托改进，而没有该框架的单个智能体在吞吐量提升33%处停滞并在数小时内不可恢复地崩溃。Arbor可推广到多代硬件平台，运行间差异在2个百分点以内，表明该方法与硬件无关且可复现。

## Abstract
Arbor is a multi-agent framework that introduces structured tree search as a cognition layer for autonomous agents operating in large, stateful action spaces. Prior autonomous optimization systems operate on isolated targets with stateless evaluation. Arbor instead maintains an explicit search tree of scored hypotheses that serves as the shared working memory across agents, evolving with every measurement, treating failures as diagnostic signal that reshapes subsequent exploration, and expanding as prior successes shift the bottleneck distribution.   We validate Arbor on full-stack LLM inference optimization, a domain where achieving peak performance has historically required coordinated effort from engineering teams across the application, framework, compiler, kernel, and hardware stack. Arbor pairs an Orchestrator agent, which drives optimization by delegating to Domain Specialists across the inference stack, with a Critic agent that safeguards stability through root-cause analysis, introspection, and measurement validation -- a checks-and-balances architecture where neither agent can unilaterally drive the system. Agent capabilities are decomposed into hard skills (domain expertise) and soft skills (coordination protocols that determine how contributions compose), enabling fully autonomous multi-day campaigns. Arbor achieves up to 193% inference throughput-latency Pareto improvement over vendor-optimized baselines, while a single agent without the harness plateaus at +33% throughput improvement and crashes irrecoverably within hours. Arbor generalizes to multiple generations of hardware platform, and run-to-run variance is within 2 percentage points demonstrating that the method is hardware-agnostic and reproducible.

---

## 论文详细总结（自动生成）

# Arbor: Tree Search as a Cognition Layer for Autonomous Agents —— 结构化总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：现有自主优化系统（如 AlphaEvolve、AVO、KernelSkill 等）通常在孤立目标上操作，使用无状态评估，无法处理跨层、有状态、动态变化的动作空间。在全栈 LLM 推理优化中，一个优化尝试可能跨越应用、框架、编译器、内核、硬件等多个层次，且每次干预都可能改变瓶颈分布，导致级联失败。传统单智能体或单层优化方法无法持续探索并有效利用失败信号。
- **动机**：需要一种结构化的记忆机制来跟踪优化状态，使智能体能在长时间、多动作序列中维持全局视角，并能从故障中提取诊断信息以引导后续搜索。
- **整体含义**：将全栈性能优化形式化为基于启发式分数树的搜索，将树作为智能体的共享工作记忆，为自主代理提供类似“认知层”的能力，可推广到其他复杂系统的自主优化。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将优化建模为**启发式树搜索**（heuristic tree search），维护一个显式搜索树 `T = (V, E)`，根节点是初始基线配置。每个节点表示一个动作及其结果（保持/回退/崩溃）。
- 搜索状态 `S_t` 包括树、候选动作队列、历史记录及当前工作分配。树动态扩展：每次成功后的重新剖析会发现新瓶颈，生成新分支。
- 多智能体架构采用**制衡机制**（checks-and-balances）：
  - **Orchestrator Agent**（协调智能体）：驱动搜索循环（剖析 → 评分 → 委托 → 门控验证 → 更新），不直接实现优化，而是动态构建领域专家智能体。
  - **Critic Agent**（批评智能体）：负责稳定性保障，包括根因分析、内省（评估回滚动作的价值）、护栏（测量完整性）、稳定性监控。
  - **Domain Specialists**（领域专家）：由 Orchestrator 在运行时动态构建，针对特定层（内核、框架、通信、编译器、调度）进行优化，输出验证过的补丁。
- 能力分解为**硬技能**（领域专业知识）和**软技能**（协调协议，如资源仲裁、委托边界、整合对方发现），使贡献能组合。
- 持久化**知识库**（Knowledge Base）跨会话积累策略和失败模式，用于更新评分先验并指导新会话。

### 关键技术细节
- **搜索循环**：深度优先，每次迭代：剖析 → 评分 → 委托实现 → 门控（精度+端到端基准） → 更新树 → 重评分。支持自动回退到最后一个已验证状态。
- **评分函数**（公式1）：
  `h(a) = [g(a) / c(a)] * (1 - r_acc) * (1 - r_crash) * m_gap + C * sqrt( (ln N) / (1 + n_cat) )`  
  其中 `g(a)` 为期望增益（预估GP时间占比×类别加速先验），`c(a)` 为估计成本（实现+重启+基准时间），`r_acc` 和 `r_crash` 为经验失败率，`m_gap`（1~2）反映到目标的剩余差距，最后一项为 UCB 探索奖励。所有项随每次动作更新。
- **重评分**：每次动作后重新剖析，更新 `g(a)`，调整分数，确保搜索适应瓶颈变化。
- **Domain Specialist 构造**：由 Orchestrator 组合任务、硬件上下文、知识库条目、先前尝试历史等成提示，生成临时智能体，而非预定义固定代理。

## 3. 实验设计：数据集、基准、对比方法

- **目标任务**：LLM 推理服务优化，优化**吞吐量–交互性**（1/TPOT，用户令牌率）帕累托前沿。
- **硬件平台**：AMD Instinct MI355X（主实验），部分实验在 MI300X（跨代验证）。
- **模型**：6个生产模型：
  - gpt-oss-120b (MXFP4)
  - DeepSeek-R1-0528 (FP8)
  - MiniMax-M2.5 (FP8)
  - GLM-5-FP8 (FP8)
  - Qwen3.5-397B-A17B (FP8)
  - Kimi-K2.5 (MXFP4)
- **基准（baseline）**：InferenceX（Semianalysis）配置，代表工程团队手工优化的结果。Arbor 从相同软件版本和配置开始。
- **对比方法**：
  - **单智能体无搜索**：单个 LLM 使用标准“提议-实现-测试”循环，无树结构、无回退机制（+33% 提升后崩溃）。
  - **无领域专家**：Orchestrator 直接实现所有修改（+30% 后耗尽动作空间）。
  - **无批评智能体**：两个独立运行，分别因精度归零和吞吐通胀导致无效提升（+12.9%、+16.5%）。
- **评价指标**：输出吞吐量（tok/s/GPU），同时门控 TTFT、TPOT、请求完成率、精度（<1%退化）。每次保持的更改后执行完整并发扫描验证。

## 4. 资源与算力

- 论文明确提到：**多天自动优化活动**，使用了**AMD Instinct MI355X 和 MI300X GPU**。具体 GPU 数量未精确说明，但涉及6个模型、多个并发运行（包括消融和跨代验证）。文中提及“GPU-aware scheduling”和“CPU-only research tasks”，暗示使用多 GPU 集群。
- 未提供总 GPU 时长、训练成本或具体实例数量。仅提及“agent quality depends on underlying LLM backends”且使用“frontier-tier reasoning LLMs (Claude Opus 4.6/4.7)”，未披露 LLM 调用成本。

## 5. 实验数量与充分性

- **主要实验**：6个模型 × 1次完整运行（含帕累托曲线），结果详实（图3）。
- **消融实验**：在 gpt-oss-120b 上进行4种消融（无DFS、无领域专家、无批评智能体两次运行），共5条曲线（图4），覆盖了关键组件。
- **跨代验证**：在 MI300X 上对3个模型进行独立优化（附录B），结果一致。
- **运行间方差**：对 Kimi-K2.5（MI355X）和 gpt-oss-120b（MI300X）各做两次独立运行，显示差异在0.6和1.6个百分点内（附录C）。
- **充分性评价**：
  - 实验覆盖了不同模型规模、量化格式、并行度（TP=1~8），具有代表性。
  - 消融实验分离了各组件贡献，且包含2次无批评智能体运行以避免偶然性。
  - 帕累托前沿报告而非单点结果，更全面。
  - **局限**：所有实验仅在 AMD 硬件上进行，未在 NVIDIA 上验证；消融仅在单一模型上运行；未进行超参数灵敏度分析（评分常数未系统调优）。总体充分但可改进。

## 6. 论文的主要结论与发现

- **结构化树搜索+多智能体制衡实现自主全栈优化**：Arbor 在6个生产模型上获得+40%~+193%吞吐量提升（并发64），远超单智能体的+33%（且崩溃）。
- **跨层交互至关重要**：最大增益来自跨层协同优化（如GLM-5-FP8从TP=8降到TP=4并优化NSA attention内核和MoE调度，提升2.9倍）。
- **批评智能体保障测量完整性**：无批评智能体时，两个独立运行均产生系统性测量错误（精度归零、吞吐通胀）。
- **树搜索+域专家使长时间稳定探索成为可能**：自动回退和重评分机制防止崩溃终止，知识库跨会话传递经验。
- **硬件无关且可复现**：跨代硬件（MI300X vs MI355X）结果一致，运行间方差<2%，表明方法稳健。

## 7. 优点

1. **问题叙述清晰**：通过真实优化案例（单次尝试跨三层）生动说明现有方法不足。
2. **架构设计新颖**：将搜索树作为智能体共享工作记忆，并结合制衡机制（Orchestrator 激进、Critic 保守），使两者相互制约。
3. **软技能/硬技能分解**：为多智能体协作提供清晰设计原则，实践中指导资源仲裁、委托、整合等行为。
4. **评分函数可解释且可更新**：结合经验风险、成本、探索奖励，并随每次动作重新计算，适应瓶颈变化。
5. **充分实验验证**：帕累托前沿、消融、跨代、运行间方差多维度评估，结论可信。
6. **实用性强**：直接降低生产系统优化的人力成本和碳足迹，降低新硬件/模型上线门槛。

## 8. 不足与局限

1. **硬件覆盖单一**：仅测试AMD GPU（MI355X及MI300X），未在NVIDIA等主流平台上验证，声称“硬件无关”需更多证据。
2. **LLM后端固定**：使用特定能力层级模型（Claude Opus 4.6/4.7），未对不同LLM进行系统比较，通用性待检验。
3. **评分常数未调优**：`mgap`、`C` 等来自早期经验，缺少正式灵敏度分析，可能影响可复现性。
4. **计算成本未量化**：未报告GPU小时数、LLM调用次数或总费用，难以评估实际开销和公平比较。
5. **实验范围受限**：消融仅在单一模型（gpt-oss-120b）上运行；跨代验证仅3个模型；未涉及训练任务或其他领域（如EDA、编译优化）。
6. **开放性问题**：与Monte Carlo Tree Search的关系讨论不足（只提及类似，但未做理论对比或实现差异解释）；未报告对不同初始化或随机种子的鲁棒性。

（完）
