---
title: "From Untrusted Input to Trusted Memory: A Systematic Study of Memory Poisoning Attacks in LLM Agents"
title_zh: 从不可信输入到可信内存：LLM智能体中内存投毒攻击的系统研究
authors: "Pritam Dash, Tongyu Ge, Aditi Jain, Tanmay Shah, Zhiwei Shang"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04329v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 对LLM智能体中记忆中毒攻击的系统研究
tldr: 针对LLM智能体的持久内存引入的投毒风险，本文系统识别了四种内存写入通道和九个结构性漏洞，并据此建立了六类攻击分类法。设计了评估基准MPBench，发现更积极读写内存的智能体更易被利用，且现有提示注入防御无法覆盖此类攻击。研究为理解和防御内存投毒攻击提供了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04329v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 823, \"height\": 275, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04329v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1604, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.04329v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 326, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.04329v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1757, \"height\": 426, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04329v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1788, \"height\": 384, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04329v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 625, \"height\": 440, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04329v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 796, \"height\": 445, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04329v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 813, \"height\": 689, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04329v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 712, \"height\": 248, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.04329v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 826, \"height\": 426, \"label\": \"Table\"}]"
motivation: 探索LLM智能体中内存投毒的漏洞及其可利用性，以提升系统安全性。
method: 识别内存写入通道和结构性漏洞，构建六类攻击分类法并设计MPBench基准。
result: 激进读写内存的智能体更易被利用，现有防御无法覆盖内存投毒攻击。
conclusion: 为理解和防御LLM智能体内存投毒攻击提供了系统性分析和评估基准。
---

## 摘要
记忆是AI智能体的核心组件，使其能够在交互中积累知识并提升性能。然而，持久化记忆引入了记忆投毒的风险，单次对抗性记忆写入即可对智能体行为产生长期影响。我们对基于LLM的智能体中的记忆投毒进行了系统研究。我们识别出四个记忆写入通道和模型能力、系统提示设计及智能体系统架构中的九个结构漏洞，这些漏洞使得这些通道可被利用。基于这些漏洞，我们开发了六类记忆投毒攻击的分类法。此外，我们设计了MPBench——一个用于评估记忆投毒攻击的基准测试，并表明设计为更积极写入和检索记忆的智能体更容易被利用。我们还发现现有的提示注入防御无法覆盖记忆投毒攻击。我们的发现为理解和缓解针对AI智能体的记忆投毒攻击提供了基础。

## Abstract
Memory is a core component of AI agents, enabling them to accumulate knowledge across interactions and improve performance. However, persistent memory introduces the risk of memory poisoning, where a single adversarial memory write can exert long-term influence over agent behavior. We present a systematic study of memory poisoning in LLM-based agents. We identify four memory write channels and nine structural vulnerabilities in model capabilities, system prompt design, and agent system architecture that make these channels exploitable. Based on these vulnerabilities, we develop a taxonomy of six classes of memory poisoning attacks. Furthermore, we design MPBench -- a benchmark for evaluating memory poisoning attacks, and show that agents designed to write and retrieve memory more aggressively are more exploitable. We also show that existing prompt injection defenses fail to cover memory poisoning attacks. Our findings provide a foundation for understanding and mitigating memory poisoning attacks against AI agents.

---

## 论文详细总结（自动生成）

好的，作为资深学术论文分析助手，我将根据您提供的论文内容，严格按照您要求的格式进行总结。

### 论文核心问题与整体含义（研究动机和背景）

*   **核心问题**：本研究系统地探讨了LLM（大语言模型）智能体中的**内存投毒攻击**问题。攻击者如何通过外部不可信输入，诱使智能体将恶意内容写入其长期记忆，从而在未来的会话中长期、持久地影响其行为。
*   **整体含义**：智能体的记忆是其长期效用的核心，但这种持久化能力也带来了新的安全风险。与需要每次会话中都存在的提示注入攻击不同，内存投毒只需一次成功的写入，便可在后续任务中持续生效，影响更隐蔽、更深远。现有研究对此威胁缺乏系统性认知，本文旨在填补这一空白。

### 论文提出的方法论

*   **核心思想**：作者将智能体内存系统形式化为一个三元组 (S, W, R)，其中S是持久存储，W是写入函数，R是检索函数。他们通过分析内存写入路径，定位系统层面（模型、提示、架构）的结构性漏洞，并基于这些漏洞对攻击进行分类。
*   **关键技术细节**：
    1.  **识别四个内存写入通道（Write Channels）**：
        *   **C1: 显式指令执行写入**：智能体直接执行像“记住：...”这样的外部指令。
        *   **C2: 系统提示驱动写入**：智能体根据系统提示中的模糊策略（如“保存重要信息”）自行判断写入内容。
        *   **C3: 压缩驱动写入**：在上下文窗口满了或会话结束时，智能体对交互历史进行总结，并将摘要写入内存。
        *   **C4: 经验到过程（Skill）写入**：智能体将一次成功的任务执行过程提炼成一个可复用的技能（Procedure）。
    2.  **识别九个结构性漏洞**：这些漏洞分布在三个层面：
        *   **模型能力层面**：指令-数据边界模糊（V-M1）、多源上下文中的来源归属失败（V-M2）。
        *   **提示设计层面**：内存写入策略规范不足（V-P1）、压缩过程无源过滤（V-P2）。
        *   **系统架构层面**：写入路径无验证（V-S1）、共享多源上下文（V-S2）、可操控的压缩触发（V-S3）、技能创建无验证（V-S4）、自我改进机制放大攻击（V-S5）。
    3.  **建立六类攻击分类法**：基于不同的利用机制和信号强度（强信号/弱信号）：
        *   **强信号攻击**：
            *   显式命令注入
            *   条件命令注入
            *   显著性驱动的压缩投毒
        *   **弱信号攻击**：
            *   策略一致的事实注入
            *   虚假先例注入
            *   技能-过程注入
*   **攻击流程（文字描述）**：
    1.  **触发写入**：攻击者通过外部数据（网页、邮件等）向智能体提供恶意载荷。
    2.  **控制内容**：利用上述漏洞，确保恶意载荷被写入持久内存。
    3.  **触发检索**：在未来的独立会话中，用户的查询触发对被污染记忆的检索，从而让智能体表现出攻击者期望的行为。

### 实验设计

*   **数据集/场景**：设计了名为 **MPBench** 的专用基准测试集，包含 **3240个** 攻击测试用例和 **2997个** 良性测试用例。这些用例覆盖了六个攻击类别和七个领域（文件操作、网页浏览、邮件、日程管理等）。
*   **Benchmark 设计**：MPBench的评估分两步：
    *   **写入阶段**：检查智能体在处理恶意输入后，其持久内存中是否包含恶意指令（攻击成功率，ASR）。
    *   **检索阶段**：在后续独立会话中，检查智能体的行为是否受被污染内存的影响（检索成功率，RSR）。
*   **对比方法**：评估了两个开源的智能体系统：**OpenClaw**（更保守的内存策略）和 **HERMES**（更激进的内存写入设计）。此外，还评估了四种现有的**提示注入防御**方法：PIGuard、DataFilter、CommandSans、PromptArmor。

### 资源与算力

*   **未明确说明**。论文中未提及训练或评估所使用的GPU型号、数量或训练时长。文中提及所有实验均使用 `GPT-OSS-120B` 模型作为基础模型，但未给出此模型的部署细节。仅能推断其使用了较大规模的计算资源，但具体信息缺失。

### 实验数量与充分性

*   **实验数量**：实验规模很大，部署了超过 **6200个** 测试用例（3240攻击 + 2997良性），并在两个不同架构的智能体系统上进行了全面评估，展示了攻击的普遍性。同时，对四种防御方法进行了多次对比，包括在原有模型和微调后的模型上测试。
*   **充分性与公平性**：
    *   **充分**：实验覆盖了提出的所有六类攻击，并分别评估了写入和检索的成功率，验证了攻击的持久性。通过对比两个设计策略不同的智能体，有力地证明了“更激进的读写策略导致更高的可攻击性”这一核心论点。
    *   **相对公平**：在对比现有防御方法时，做了**开箱即用**和**重新训练/适配**两种情况的测试，避免了仅比较原始未调优模型的片面性。评估标准清晰（ASR, RSR, TPR, FPR）。
    *   **潜在偏差**：实验仅使用了单一模型（GPT-OSS-120B），未探索其他模型（如开源模型或不同规模的模型）下的表现，结果的泛化性有待验证。另外，测试用例有一部分通过“标记上下文块”方式交付，而非完整的工具调用流程，与实际部署场景存在差异。

### 论文主要结论与发现

1.  **攻击的有效性与持久性**：内存投毒攻击极为有效，在OpenClaw和HERMES上的平均攻击成功率为50.46%，检索成功率为41.05%。成功写入的内存会显著影响后续的智能体行为（持久性）。
2.  **设计选择与可攻击性的权衡**：那些旨在提升智能体长期任务性能的设计（如更频繁的读写、模糊的写入策略），恰恰**扩大了攻击面**。HERMES（更激进）在所有攻击类别上的成功率均远高于OpenClaw。
3.  **现有防御的失效**：现有的基于**提示注入**的防御在防御内存投毒方面**覆盖严重不足**。尤其是对“弱信号”攻击，其有效检测率远低于“强信号”攻击。这是因为内存投毒的核心是让恶意内容看起来像是“正常数据”，而非包含显著的攻击指令。即使对防御器进行针对性的再训练，其效果提升也非常有限。

### 优点

*   **系统性与结构性**：本文是第一个对LLM智能体内存投毒进行系统性研究的先驱工作。它从“写入通道”和“结构性漏洞”的角度构建了一个清晰的分析框架，而非简单罗列攻击案例，为后续研究提供了坚实的基础。
*   **攻击分类法**：提出的六类攻击分类法涵盖全面，且清晰地关联了漏洞与攻击方法，使得分析具有理论指导性。
*   **专用基准MPBench**：设计并公开了第一个专门针对内存投毒攻击的基准测试，填补了现有安全基准（如AgentDojo）只关注单会话提示注入的空白。
*   **揭示了核心矛盾**：明确指出了“内存能力”与“内存安全”之间的内在权衡，这对智能体设计者具有重要的警示和指导意义。
*   **消解了常见的错误假设**：通过实验有力证明了“使用已知的提示注入防御就足以防护内存投毒”这一假设是错误的，并清晰地解释了其原因。

### 不足与局限

*   **模型单一性**：所有实验仅在 `GPT-OSS-120B` 一个模型上进行，未测试其他模型（如不同系列、不同参数量、不同训练数据的模型），其结论的普适性受限。不同模型的指令遵循能力、混淆容忍度等可能存在差异。
*   **环境仿真程度**：MPBench部分测试用例采用了“标记上下文块”的交付方式，而非完整的工具调用抓取流程。这虽然是一种常见的简化方法，但可能无法完全模拟真实世界中工具返回数据的复杂性和多样性（如格式、延迟、错误处理等）。
*   **攻击表面的简化**：威胁模型假设攻击者不能修改系统提示、不能冒充用户，且无法访问智能体内存的中间状态。虽然这定义了“外部标准”攻击者，但现实中存在更复杂的攻击场景（如多用户共享内存的横向渗透），这些被排除在范围之外。
*   **防御评估的局限性**：仅评估了四种提示注入防御，可能未涵盖所有主流或最新防御方案。此外，对于论文自身提出的防御方向（如写入路径验证、内存加固等），并未进行实验验证其有效性，仅作为讨论提出。

### 未来研究方向与开放问题

*   **更广泛的模型与系统评估**：论文仅在单一模型 (`GPT-OSS-120B`) 上验证，未来应在更多不同规模、不同架构的闭源及开源模型（如 LLaMA、Qwen、GPT-4 系列）上重复实验，以确认攻击的普遍性与模型间的差异。
*   **防御机制的设计与实现**：论文仅从路线层面提出了防御方向（如写入路径验证、内存加固、来源属性追踪），但未具体实现或实验验证。未来应开发实用的防御方案，例如：
    *   为内存写入操作增加显式验证步骤（用户确认或规则审核）。
    *   设计基于内容来源的隔离策略，确保外部数据无法覆盖系统级指令。
    *   利用一致性检查或异常检测模型识别潜在投毒内容。
*   **更复杂的攻击场景**：当前威胁模型限定了攻击者身份（不能修改系统提示、不能冒充用户、不能访问内存内部状态）。后续可研究：
    *   **多用户环境**：攻击者利用共享内存（如团队协作智能体）污染其他用户的上下文。
    *   **渐进式投毒**：通过多次细微注入逐步改变内存，避免被一次性检测。
    *   **自我改进循环的放大**：攻击者利用智能体的经验-技能写入通道，注入一个看似有用的技能，该技能在后续任务中被自动复用并进一步污染其他内存单元。
*   **内存系统的审计与可解释性**：如何检测内存是否被投毒？如何追溯污染源？开发内存审计工具（如内存版本控制、变更日志）和入侵检测系统（基于统计或语义）是重要的实用方向。
*   **对抗性训练与鲁棒记忆策略**：探索通过对抗性训练提升智能体对投毒指令的辨别能力，以及设计更安全的默认内存策略（如限制写入频率、要求用户二次确认、对写入内容进行自动摘要并标注来源）。
*   **标准化安全的演进**：MPBench 是首个专门针对内存投毒的基准，未来应扩展至更真实的交互场景（如完整工具调用流程、多轮对话），并整合防御评估作为必选组件，形成社区公认的安全基准。

（完）
