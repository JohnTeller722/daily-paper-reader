---
title: Deployment-Time Memorization in Foundation-Model Agents
title_zh: 基础模型代理中的部署时记忆化
authors: "Lei, Chen, Guilin Zhang, Kai Zhao, Dalmo Cirne, Andy Olsen, Xu Chu, Zeke Miller, Alet Blanken, Amine Anoun, Jerry Ting"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10062v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 研究部署时记忆化、隐私-效用边界和遗忘残留
tldr: "基础模型代理在部署时会持久记忆用户交互，但现有研究未考虑记忆设计对隐私和效用的联合影响。本文系统评估了三个记忆旋钮：摘要攻击性、检索宽度和删除模式。关键发现：关键事实摘要将提取率降低76%（Gemma 3 12B）和64%（GPT-4o-mini），几乎不损失个性化召回；但仅删除原始数据无法清除派生摘要，约20%实例中残留可恢复。结果表明必须将代理记忆视为一类独立的记忆化机制进行评测。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10062v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 819, \"height\": 967, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10062v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 556, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10062v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1497, \"height\": 542, \"label\": \"Table\"}]"
motivation: 现有工作仅关注参数化记忆或审计固定记忆配置，未刻画记忆设计选择如何联合影响个性化效用、提取风险和删除保真度。
method: 提出部署时记忆化概念，用个性化召回和对抗提取率度量隐私-效用边界，扫描摘要攻击性、检索宽度k和删除模式三个旋钮，并引入遗忘残留分数量化已删除信息的可恢复性。
result: "关键事实摘要减少76%（Gemma 3 12B）和64%（GPT-4o-mini）的提取，几乎保持全部个性化召回；但仅原始数据删除导致约20%实例中派生摘要残留，需全流水线清除或墓碑编辑才能清零。"
conclusion: 持久代理记忆必须作为一流的记忆化机制评估，需关注其帮助召回的内容、可提取的内容以及能真正擦除的内容。
---

## 摘要
基础模型代理正日益成为跨交互记忆用户的长期系统，使得记忆化成为明确的部署时功能，而不仅仅是模型权重的属性。现有工作解决参数化记忆化或审计固定记忆配置，但没有描述记忆设计选择如何共同塑造个性化效用、提取风险和删除保真度。我们将这一表面研究为部署时记忆化，将代理记忆表述为通过个性化召回率（PR）和对抗提取率（AER）衡量的隐私-效用边界，并扫描三个记忆设计旋钮：总结攻击性、检索广度（k）和删除模式。我们进一步引入遗忘残留分数（FRS）来量化删除信息是否可以从派生记忆层中恢复。在LongMemEval上，关键事实总结将Gemma 3 12B上的金丝雀提取减少了76%，GPT-4o-mini上减少了64%，同时几乎保留了所有个性化召回率；关键是，一旦内容被压缩掉，增加k不再恢复泄露。然而，相同的压缩导致了删除保真度失败：仅原始删除使派生摘要副本在大约20%的实例中可恢复，只有全管道清除或墓碑修订才能将最差层残留降至零。这些结果共同表明，持久代理记忆必须作为一流的记忆化机制进行评估——通过它帮助代理回忆的内容、它使什么变得可提取以及它真正能擦除什么来评估。

## Abstract
Foundation-model agents are increasingly long-lived systems that remember users across interactions, making memorization an explicit deployment-time function rather than solely a property of model weights. Existing work addresses parametric memorization or audits fixed memory configurations, but does not characterize how memory-design choices jointly shape personalization utility, extraction risk, and deletion fidelity. We study this surface as deployment-time memorization, formulating agent memory as a privacy-utility frontier measured by Personalization Recall (PR) and Adversarial Extraction Rate (AER), and sweeping three memory-design knobs: summarization aggressiveness, retrieval breadth (k), and deletion mode. We further introduce the Forgetting Residue Score (FRS) to quantify whether deleted information remains recoverable from derived memory tiers. On LongMemEval, key-fact summarization reduces canary extraction by 76% on Gemma 3 12B and 64% on GPT-4o-mini while preserving nearly all personalization recall; critically, once content is compressed away, increasing k no longer restores leakage. The same compression, however, induces a deletion-fidelity failure: raw-only deletion leaves derived summary copies recoverable in approximately 20% of instances, and only full-pipeline purge or tombstone redaction drives worst-tier residue to zero. Together, these results establish that persistent agent memory must be evaluated as a first-class memorization mechanism -- assessed by what it helps agents recall, what it makes extractable, and what it can truly erase.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化中文总结。

# 论文总结：基础模型代理中的部署时记忆化

## 1. 论文的核心问题与整体含义

- **研究动机与背景**：随着基础模型代理（Agent）从无状态助手演变为长期记忆用户交互的系统，记忆化不再仅仅是模型权重在训练时产生的附带属性，而是成为一项明确的**部署时（Deployment-Time）** 系统功能。现有研究主要关注参数化记忆化（训练数据泄露）或审计固定的记忆配置，但未能系统性地描述记忆系统的设计选择（如如何总结、检索和删除）如何共同影响**个性化效用（Utility）**、**信息提取风险**和**删除保真度**。
- **核心问题**：如何理解和量化为追求个性化而设计的持久性代理记忆系统其内在的隐私-效用权衡，特别是当信息被复制到不同记忆层级后，简单的“删除”操作是否真的有效。

## 2. 论文提出的方法论

- **核心思想**：将代理记忆系统视为一个独立的部署时记忆化机制，并引入**隐私-效用边界（Privacy-Utility Frontier）** 的概念。该边界由两个关键指标定义，并通过扫描三个核心记忆设计参数来刻画。
- **关键技术细节**：
    - **代理记忆管道（Agent-Memory Pipeline）**：一个写-检索-响应的流程。每次会话后写入长期记忆，查询时通过余弦相似度检索最相关的`k`个记忆块注入到上下文中。
    - **三个记忆设计旋钮**：
        1.  **总结攻击性（S）**：控制存储的内容。`S=0`（原始对话轮次），`S=1`（关键个人事实），`S=2`（单句会话总结）。
        2.  **检索广度（k）**：控制注入上下文的记忆块数量（1, 3, 6, 10, 25）。
        3.  **删除模式**：定义如何响应用户的“遗忘”请求，形成一个五级递进梯子：`noop`（无操作）、`raw only`（仅删除原始文本）、`raw plus resummarize`（删除原始文本并重新总结）、`full purge`（全管道清除）、`tombstone`（墓碑修订，替换为`[REDACTED]`）。
- **关键公式与度量（文字说明）**：
    - **个性化召回率（PR）**: 衡量代理能否正确回答需要记忆用户上下文的问题。
    - **对抗提取率（AER）**: 衡量攻击者能否通过直接、间接或越狱（jailbreak）提示，从代理响应中提取出注入的“金丝雀”（高熵秘密）。
    - **隐私-效用AUC（PUA）**: 在给定总结级别`S`下，通过扫描`k`值绘制出的PR-AER曲线下面积，用于综合评估一个总结策略的优劣。
    - **遗忘残留分数（FRS）**: 核心创新指标。用于量化在删除操作后，秘密信息是否仍能从不同记忆层级（原始层、摘要层）中恢复出来。`FRS_worst`表示最差层级上的残留率。

## 3. 实验设计

- **数据集与场景**：
    - 使用了**LongMemEval**数据集中的 oracle 子集，这是一个包含多会话聊天历史和问答对的标准评测基准，用于测试长期用户上下文记忆。
- **基准（Benchmark）**：该研究本质上是在评测不同记忆系统设计（旋钮组合）下的表现，因此其基准是`S=0`（原始记忆）和不同的`k`值，以及`noop`删除模式。
- **对比方法**：
    - **模型层面**：对比了**Gemma 3 12B**（开源）和**GPT-4o-mini**（闭源）两种模型，以区分管道级效果和模型本身（如RLHF）导致的差异。
    - **旋钮层面**：进行了全因子扫描（3个S值 × 5个k值）和5种删除模式在3个S值下的对比。同时，还分解了不同攻击提示（直接、间接、越狱）下的AER。

## 4. 资源与算力

- **明确说明**：论文中**未明确提及**所使用的具体算力资源，如GPU型号、数量或训练/推理时长。
- **推测**：实验主要基于API调用（GPT-4o-mini）和本地部署（Gemma 3 12B via Ollama），推测算力需求不高，属于中等规模的研究。

## 5. 实验数量与充分性

- **实验数量**：
    - 从论文描述看，实验在50个实例（N=50）的全因子配置下运行。
    - 主要实验包括：隐私-效用边界扫描（3种S × 5种k × 2种模型 = 30组主要结果），遗忘残留评估（3种S × 5种删除模式 × 2种模型 = 30组结果），以及探针分解等。共进行了约60组核心实验。
- **充分性与客观性**：
    - **优点**：实验设计较为系统，通过全因子扫描清晰地揭示了各个旋钮的独立和交互效应。使用了两个不同的模型（开源和闭源），增强了结论的鲁棒性和普适性。注入合成金丝雀的方法能有效隔离训练时的记忆化，聚焦于部署时记忆化。
    - **局限性**：
        - 数据集规模较小（`N=50`），虽然结合了全因子设计，但样本量有限可能导致结果置信区间较宽，泛化性有待在更大规模数据集上验证。
        - 仅测试了单次交互中的单一秘密，未模拟更复杂的多秘密、长期记忆累积或动态更新的场景。
        - 现实的隐私攻防场景可能更为复杂，论文中的攻击模型（直接、间接、越狱）虽然覆盖了主要类型，但可能不够全面。

## 6. 论文的主要结论与发现

1.  **总结是隐私-效用边界的关键杠杆**：采用**关键事实总结（`S=1`）** 能在几乎不牺牲个性化召回率（PR仅下降约5%）的前提下，将对抗提取率（AER）大幅降低**76%（Gemma）** 和**64%（GPT-4o-mini）**。
2.  **检索广度（k）在总结后是隐私中性的**：在原始记忆（`S=0`）下，增大k会同时提升PR和AER，形成艰难的权衡。但在使用总结后（`S≥1`），增加k不再显著增加AER，因为秘密已被“清洗”出可检索的记忆表征中。
3.  **简单删除是不安全的**：仅删除原始记录（`raw only`）会导致秘密信息在派生的摘要层中保持可恢复，残留率约为**20%**。即使重新总结（`raw plus resummarize`）也无法完全清除残留。
4.  **完全擦除需要全管道清除**：只有**全管道清除（`full purge`）** 或**墓碑修订（`tombstone`）** 才能将最差层级残留分数（`FRS_worst`）降至零，确保信息被真正“遗忘”。

## 7. 优点

- **视角新颖**：首次系统地定义和量化了“部署时记忆化”这一核心问题，将其与传统的训练时记忆化区分开来，具有重要的理论价值和实践指导意义。
- **实用导向**：研究出的实用配方（关键事实总结 + 中等k + 层级感知的删除）直接为工程实践提供了可操作的指导。
- **度量创新**：引入遗忘残留分数（FRS），为评估删除操作的有效性提供了一个精准的量化工具，揭示了“删除”操作在多层记忆系统中的复杂性。
- **实验设计严谨**：通过跨模型（开源 vs. 闭源）、多旋钮、全因子的实验设计，有力地证明了其发现是系统管道级别的效应，而非特定模型的特性。

## 8. 不足与局限

- **数据集规模有限**：实验基于50个实例，虽然在学术评测中可接受，但结论的统计显著性和泛化性有待在大规模、多样化数据集上进一步验证。
- **未考虑多维秘密与长期演化**：实验只模拟了单次交互写入一个秘密，未深入探讨多个秘密同时存在、用户偏好随时间动态变化、以及信息在多次总结和检索中的信息扭曲与复制问题。
- **攻击模型相对理想化**：论文假设攻击者可以无限次查询代理，但无法访问内存。现实中可能存在更复杂的攻击方式，如发起中间人攻击或利用代理的插件能力。
- **FRS的通用性**：FRS目前只针对特定管道定义，其在不同记忆架构（如不同的向量数据库、不同的总结模型）下的适用性和迁移性有待探索。
- **伦理与安全评估**：论文虽然涉及隐私，但未深入讨论潜在的双重使用风险，即研究结果本身可能被不道德的一方用于设计更难以删除的记忆系统。

（完）
